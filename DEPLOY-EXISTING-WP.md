# 既存のWordPressに子テーマをアップロードする手順

既存のWordPressインストールに `tokimeki-child` テーマをアップロードする方法です。

## 📋 前提条件

- ✅ 既存のWordPressがサブドメインにインストールされている
- ✅ 親テーマ「Twenty Twenty-Four」がインストールされている（またはインストール可能）
- ✅ FTP/SFTPまたはSSH接続が可能

## 🎯 デプロイ方法

### 方法1: rsyncスクリプトを使った自動デプロイ（SSH接続可能な場合）

#### 手順

1. **デプロイスクリプトの設定**

   `deploy-theme.sh` を開いて、サーバー情報を設定：

   ```bash
   STAGING_HOST="staging.example.com"           # サブドメインのホスト名
   STAGING_USER="your-username"                  # SSHユーザー名
   STAGING_WP_PATH="/var/www/html/staging"      # WordPressのルートパス
   ```

2. **実行権限の付与**

   ```bash
   chmod +x deploy-theme.sh
   ```

3. **デプロイ実行**

   ```bash
   ./deploy-theme.sh
   ```

4. **WordPress管理画面でテーマを有効化**

   - WordPress管理画面にログイン
   - **外観 → テーマ** に移動
   - **TOKIMEKI Child** テーマを探して「有効化」をクリック

---

### 方法2: FTP/SFTPクライアントを使った手動デプロイ

#### 手順

1. **FTP/SFTPクライアントを起動**

   推奨ツール：
   - FileZilla（無料）
   - Cyberduck（無料）
   - Transmit（有料、Mac専用）

2. **サーバーに接続**

   - ホスト名：サブドメインのFTPサーバー
   - ユーザー名・パスワード：サーバー提供者から取得

3. **テーマフォルダをアップロード**

   **ローカル側：**
   ```
   /Applications/MAMP/htdocs/tokimeki/wp-content/themes/tokimeki-child/
   ```

   **リモート側：**
   ```
   /wp-content/themes/tokimeki-child/
   ```

4. **アップロードするファイル・フォルダ**

   以下のすべてをアップロード：
   - `assets/` フォルダ全体
   - `parts/` フォルダ全体
   - `templates/` フォルダ全体
   - `functions.php`
   - `style.css`
   - `theme.json`

5. **ファイル権限の設定**

   - フォルダ：**755**
   - ファイル：**644**

6. **WordPress管理画面でテーマを有効化**

   - WordPress管理画面にログイン
   - **外観 → テーマ** に移動
   - **TOKIMEKI Child** テーマを探して「有効化」をクリック

---

## ✅ デプロイ後の確認事項

### 1. 親テーマの確認

- [ ] 「Twenty Twenty-Four」テーマがインストールされているか確認
- [ ] インストールされていない場合は、WordPress管理画面からインストール

### 2. テーマの有効化

- [ ] WordPress管理画面 → **外観 → テーマ**
- [ ] **TOKIMEKI Child** テーマを有効化

### 3. サイトの動作確認

- [ ] フロントページが正しく表示されるか
- [ ] CSS/JSが正しく読み込まれているか（ブラウザの開発者ツールで確認）
- [ ] 画像が正しく表示されるか
- [ ] ナビゲーションメニューが機能しているか

### 4. エラーの確認

- [ ] WordPress管理画面にエラーが表示されていないか
- [ ] ブラウザのコンソールにエラーが表示されていないか
- [ ] サーバーのエラーログを確認（必要に応じて）

### 5. キャッシュのクリア

- [ ] ブラウザのキャッシュをクリア
- [ ] WordPressのキャッシュプラグインをクリア（使用している場合）

---

## 🔧 トラブルシューティング

### テーマが表示されない

**原因：**
- ファイルが正しくアップロードされていない
- ファイルのパスが間違っている
- ファイル権限が正しくない

**解決方法：**
1. FTPクライアントで `wp-content/themes/tokimeki-child/` フォルダが存在するか確認
2. `style.css` ファイルが存在するか確認
3. ファイル権限を確認（フォルダ：755、ファイル：644）

### CSS/JSが読み込まれない

**原因：**
- ファイルパスが間違っている
- ファイルがアップロードされていない
- キャッシュの問題

**解決方法：**
1. ブラウザの開発者ツール（F12）でネットワークタブを確認
2. 404エラーが出ているファイルを確認
3. `functions.php` のパス設定を確認
4. ブラウザのキャッシュをクリア

### 親テーマが見つからないエラー

**原因：**
- 「Twenty Twenty-Four」テーマがインストールされていない

**解決方法：**
1. WordPress管理画面 → **外観 → テーマ**
2. 「新規追加」をクリック
3. 「Twenty Twenty-Four」を検索してインストール

### 白い画面が表示される

**原因：**
- PHPエラーが発生している
- `functions.php` に構文エラーがある

**解決方法：**
1. WordPressのデバッグモードを有効化（`wp-config.php` に以下を追加）：
   ```php
   define('WP_DEBUG', true);
   define('WP_DEBUG_LOG', true);
   define('WP_DEBUG_DISPLAY', false);
   ```
2. `wp-content/debug.log` を確認
3. エラー内容に応じて修正

---

## 📝 重要な注意事項

### ⚠️ 既存のデータについて

- このデプロイは**テーマファイルのみ**をアップロードします
- 既存の投稿、ページ、メディアファイルは影響を受けません
- 既存のプラグイン設定も保持されます

### ⚠️ バックアップの推奨

デプロイ前に以下をバックアップすることを推奨します：

1. **データベースのバックアップ**
   - phpMyAdminからエクスポート
   - または、バックアッププラグインを使用

2. **既存テーマのバックアップ**（使用している場合）
   - `wp-content/themes/` フォルダをコピー

### ⚠️ 本番環境との違い

- ステージング環境なので、必要に応じてテスト用のデータを使用
- 本番環境のデータベース情報は使用しないでください

---

## 🚀 次のステップ

デプロイが完了し、動作確認が取れたら：

1. **クライアントに共有**
   - ステージングサイトのURLを共有
   - ログイン情報を共有（必要に応じて）

2. **フィードバックを収集**
   - クライアントからのフィードバックを待つ
   - 必要に応じて修正を加える

3. **本番環境への準備**
   - 問題がなければ本番環境へのデプロイ準備
   - 本番環境でも同じ手順でデプロイ可能

---

## 📞 サポート

問題が発生した場合：

1. エラーメッセージを確認
2. サーバーのエラーログを確認
3. WordPressのデバッグログを確認
4. 必要に応じて開発者に連絡

