# FTP/SFTPクライアントを使ったデプロイ手順

SSH接続ができない場合の、FTP/SFTPクライアントを使った手動デプロイ方法です。

## 📋 必要な情報

サーバー提供者から以下を確認してください：
- **FTP/SFTPサーバーアドレス**（通常は `ftp.sv6007.wpx.ne.jp` または `sv6007.wpx.ne.jp`）
- **FTPユーザー名**（通常は `wp020865`）
- **FTPパスワード**
- **ポート番号**（FTP: 21、SFTP: 22）

## 🛠️ 手順

### ステップ1: FileZillaをダウンロード・インストール

1. FileZillaをダウンロード
   - https://filezilla-project.org/download.php?type=client
   - Mac版をダウンロードしてインストール

### ステップ2: サーバーに接続

1. FileZillaを起動
2. 上部の「ホスト」「ユーザー名」「パスワード」「ポート」を入力：
   ```
   ホスト: sv6007.wpx.ne.jp（または ftp.sv6007.wpx.ne.jp）
   ユーザー名: wp020865
   パスワード: （サーバー提供者から取得）
   ポート: 21（FTP）または 22（SFTP）
   ```
3. 「クイック接続」をクリック

### ステップ3: WordPressのパスを確認

接続後、リモート側（右側）で以下を確認：

1. **`wp-config.php` ファイルを探す**
   - リモート側のフォルダを移動しながら `wp-config.php` を探す
   - 通常は以下のいずれかのパスにあります：
     - `/home/wp020865/public_html/`
     - `/home/wp020865/www/`
     - `/home/wp020865/htdocs/`
     - `/home/wp020865/`（直下の場合もある）

2. **WordPressのルートパスを確認**
   - `wp-config.php` があるフォルダがWordPressのルートパス
   - そのフォルダ内に `wp-content/` フォルダがあることを確認

### ステップ4: テーマフォルダをアップロード

1. **リモート側でテーマフォルダに移動**
   - WordPressのルートパス → `wp-content/` → `themes/` に移動
   - 既に `tokimeki-child` フォルダがある場合は、その中身を確認

2. **ローカル側でテーマフォルダを開く**
   - 左側（ローカル）で以下に移動：
     ```
     /Applications/MAMP/htdocs/tokimeki/wp-content/themes/tokimeki-child/
     ```

3. **フォルダ全体をアップロード**
   - ローカル側の `tokimeki-child` フォルダを選択
   - リモート側の `themes/` フォルダにドラッグ&ドロップ
   - 既存ファイルがある場合は「上書き」を選択

4. **アップロードするファイル・フォルダ**
   - `assets/` フォルダ全体
   - `parts/` フォルダ全体
   - `templates/` フォルダ全体
   - `functions.php`
   - `style.css`
   - `theme.json`

### ステップ5: ファイル権限の設定

アップロード後、ファイル権限を確認・設定：

1. **リモート側で `tokimeki-child` フォルダを右クリック**
2. **「ファイルの権限」を選択**
3. **権限を設定**：
   - フォルダ：**755**
   - ファイル：**644**

または、すべてのファイルを選択して一括設定：
- フォルダ：755
- ファイル：644

### ステップ6: WordPress管理画面でテーマを有効化

1. **WordPress管理画面にログイン**
   - ブラウザでサイトにアクセス
   - `/wp-admin/` にアクセスしてログイン

2. **テーマを有効化**
   - **外観 → テーマ** に移動
   - **TOKIMEKI Child** テーマを探す
   - 「有効化」をクリック

3. **親テーマの確認**
   - 「Twenty Twenty-Four」テーマがインストールされているか確認
   - なければ、**外観 → テーマ → 新規追加** からインストール

## ✅ 確認事項

デプロイ後、以下を確認：

- [ ] すべてのファイルが正しくアップロードされているか
- [ ] ファイル権限が正しいか（フォルダ：755、ファイル：644）
- [ ] WordPress管理画面でテーマが表示されるか
- [ ] テーマを有効化できるか
- [ ] フロントページが正しく表示されるか
- [ ] CSS/JSが正しく読み込まれているか（ブラウザの開発者ツールで確認）

## 🆘 トラブルシューティング

### 接続できない

**原因：**
- ホスト名が間違っている
- ポート番号が間違っている
- ファイアウォールでブロックされている

**解決方法：**
- サーバー提供者にFTP接続情報を確認
- ポート番号を変更（21 → 22、またはその逆）

### ファイルがアップロードできない

**原因：**
- ファイル権限の問題
- ディスク容量不足

**解決方法：**
- サーバー提供者に連絡
- ファイルマネージャー（cPanelなど）からアップロードを試す

### テーマが表示されない

**原因：**
- ファイルパスが間違っている
- `style.css` が正しくアップロードされていない

**解決方法：**
- `wp-content/themes/tokimeki-child/style.css` が存在するか確認
- ファイルの内容を確認（正しくアップロードされているか）

## 📝 次のステップ

1. FileZillaでサーバーに接続
2. WordPressのパスを確認
3. テーマフォルダをアップロード
4. WordPress管理画面でテーマを有効化
5. サイトを確認してクライアントに共有

